{% extends 'email/base.html.twig' %}

{% block body %}
    <h1 style="color: #d4a024; margin-bottom: 20px;">{{ 'password_change_confirm.title' | trans }}</h1>

    <p style="font-size: 16px; line-height: 1.6; color: #333;">
        {{ 'password_change_confirm.hello' | trans }}{{ user.firstName ? ', ' ~ user.firstName : '' }},
    </p>

    <p style="font-size: 16px; line-height: 1.6; color: #333;">
        {{ 'password_change_confirm.request' | trans|raw }}
    </p>

    <p style="font-size: 16px; line-height: 1.6; color: #333;">
        {{ 'password_change_confirm.instructions' | trans }}
    </p>

    <div style="text-align: center; margin: 30px 0;">
        <a href="{{ url('app_confirm_password_change', {token: changeToken}) }}"
           style="display: inline-block; padding: 15px 40px; background-color: #d4a024; color: #ffffff; text-decoration: none; border-radius: 5px; font-size: 16px; font-weight: bold;">
            {{ 'password_change_confirm.button' | trans }}
        </a>
    </div>

    <p style="font-size: 14px; line-height: 1.6; color: #666; margin-top: 30px;">
        {{ 'password_change_confirm.link_help' | trans }}
    </p>
    <p style="font-size: 12px; color: #999; word-break: break-all;">
        {{ url('app_confirm_password_change', {token: changeToken}) }}
    </p>

    <div style="background-color: #fff3cd; border-left: 4px solid #ffc107; padding: 15px; margin: 30px 0;">
        <p style="margin: 0; color: #856404; font-size: 14px;">
            <strong>⚠️ {{ 'password_change_confirm.important' | trans }}:</strong> {{ 'password_change_confirm.link_expiry' | trans }}
        </p>
    </div>

    <p style="font-size: 14px; line-height: 1.6; color: #666; margin-top: 30px;">
        {{ 'password_change_confirm.ignore' | trans }}
    </p>

    <p style="font-size: 14px; line-height: 1.6; color: #666;">
        {{ 'password_change_confirm.regards' | trans }}<br>
        <strong>{{ 'password_change_confirm.team' | trans }}</strong>
    </p>
{% endblock %}
